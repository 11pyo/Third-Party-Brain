# Contributing / 기여 가이드

Thanks for your interest! / 관심 가져주셔서 감사합니다!

## How to contribute

Most contributions start in **Discussions**:
- ❓ **Q&A** — questions about applying/replicating the pattern
- 💡 **Ideas** — improvements to search / intake / scaling thresholds
- 🛠️ **Show and tell** — archives you replicated in your own domain

For doc fixes or methodology improvements, **Pull Requests** are welcome. Please:
1. **No real data** — never include credentials, internal IPs, personal info, or real operational records (see `SECURITY.md`).
2. **Keep it generic** — this is a reusable blueprint; avoid org-specific identifiers.
3. **Keep doc pairs in sync** — every topic has a `*.ai.md` (for AI agents) **and** a `*.human.md` (for people). Update both.
4. **Log program/structure changes** — if you change tools, algorithms, file layout, or encoding/deploy methods, add an entry to `archive-blueprint/CHANGELOG.*` (newest on top). Content-only edits don't need this.

## What makes a good contribution
- A clear replication case in a new domain (MM, FI, HR, a general wiki…)
- A concrete search/intake improvement with the problem it solves
- Fixes that keep the blueprint accurate and reproducible

---

## 기여 방법

대부분의 기여는 **Discussions**에서 시작합니다:
- ❓ **질문(Q&A)** — 적용·복제 관련 질문
- 💡 **아이디어(Ideas)** — 검색·인테이크·확장 임계점 개선 제안
- 🛠️ **사례 공유(Show and tell)** — 본인 도메인으로 복제한 결과

문서 수정·방법론 개선은 **Pull Request** 환영합니다. 다만:
1. **실데이터 금지** — 계정·내부 IP·개인정보·실제 운영기록 포함 금지 (`SECURITY.md` 참조).
2. **일반화 유지** — 재사용 가능한 블루프린트이므로 조직 고유 식별자 배제.
3. **문서 쌍 동기화** — 모든 주제는 `*.ai.md`(AI용) **+** `*.human.md`(사람용) 쌍입니다. 둘 다 갱신하세요.
4. **프로그램·구조 변경은 기록** — 도구·알고리즘·파일구성·인코딩/배포 방식을 바꾸면 `archive-blueprint/CHANGELOG.*`에 항목 추가(최신이 맨 위). 내용만 수정은 불필요.

## 좋은 기여란
- 새 도메인(MM·FI·HR·일반 위키 등)의 명확한 복제 사례
- 해결한 문제가 분명한 검색·인테이크 개선
- 블루프린트를 정확·재현 가능하게 유지하는 수정
